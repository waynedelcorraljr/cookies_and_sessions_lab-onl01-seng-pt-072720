<h1>Cart</h1>
 
<% cart.each do |item| %>
    <ul>
    <li><%= item %></li>
    </ul>
<% end %>

<%= form_tag url: "/", method: :post do %> 
    <%= text_field_tag :product %>
    <%= submit_tag "add to cart" %>
<% end %>
